# Main entry. Compose model + data + trainer (method) + train hyperparameters.
# Override on the CLI, or pick a full preset with `+experiment=unlearn/wmdp`.
defaults:
  - model: llada
  - data: wmdp_bio
  - trainer: cap
  - eval: wmdp
  - _self_
