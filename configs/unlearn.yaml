# Train entry. Defaults give a runnable recipe; `experiment=` swaps the whole
# preset (model + data + trainer + eval + dataset-level args).
defaults:
  - model: llada
  - data: wmdp_bio
  - trainer: cap
  - eval: wmdp
  - experiment: null
  - _self_
